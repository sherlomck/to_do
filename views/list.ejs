<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="stylesheets/styles.css">
    <title>To-Do List</title>
</head>
<body>
    <div class="box" id="heading">
        <h1><%=list_title%></h1>
    </div>
    <div class="box">
        <% items.map(item => { %>
        <form action="/delete" method="post">
        <div class="item">
            <input type="checkbox" name="checkbox" value="<%= item._id %>" onchange="this.form.submit()">
            <p><%= item.name %></p>
        </div>
        <input type="hidden" name="listName" value="<%= list_title %>">
        </form>
        <% }) %>
        <form class="item" action="/input" method="post">
            <input type="text" name="fill" placeholder="New Item" autocomplete="off">
            <button type="submit" name="list" value="<%= list_title %>">+</button>
        </form>
    </div>
    
</body>
</html>